# Let's Cook AI

Modern premium AI agency landing page built as a single-page React component with Tailwind CSS.

## Run

```bash
npm install
npm run dev
```

The main component lives at `src/App.jsx`.

For this workspace, a no-install static visual preview is also available at `preview.html`.
